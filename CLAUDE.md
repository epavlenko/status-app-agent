# CLAUDE.md

> **Progressive disclosure:** this file is loaded into context every session — keep it short. Read linked docs only when your task requires them.

Read `AGENTS.md` for project instructions (architecture, build, patterns, gotchas).

## Preferences

- Always use `uv` — never `pip` directly
- Use `uv run pytest` for tests

## Knowledge Base

**Vault**: `$OBSIDIAN_VAULT_PATH` — read `PROJECTS_REGISTRY.yaml` in vault root for paths, notes, and cross-project navigation.

## Claude Code Specific

### Hooks

<!-- No hooks configured yet. -->

### Skills

<!-- No skills configured yet. -->
